Never modify domain models from infrastructure.

Never bypass EventBus.

Everything is a Plugin.

Every module has tests.

No circular imports.

No singleton.

No global state.

No magic constants.

Every public class has docstrings.

Every commit must pass Ruff.

Every feature must include unit tests.

Coverage minimum 90%.

Never use print().

Use structlog.

Use pathlib.

No os.path.

No hardcoded paths.

Always inject dependencies.

Repositories expose domain entities only.
